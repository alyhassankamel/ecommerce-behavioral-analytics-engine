version: "3.9"

services:
  mongodb:
    image: mongo:latest
    container_name: ecommerce_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ecommerce
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
